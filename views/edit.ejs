<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/root.css">
    <link rel="stylesheet" href="/styles/cardForm.css">
    <link rel="stylesheet" href="/styles/passwordCheck.css">
    <title>Edit</title>
</head>
<body>
    <%- include("partials/navbar") %>
    <h1>Edit Card Details</h1>
    <strong class="info-text">Lines with <img src="/images/non-edit.svg" class="helper-img" alt=""> cannot be changed.</strong>
    <% if (locals.error && error.type === "password") { %>
        <div class="helper-text">
          <h2>Incorrect Password.</h2>
          <p><%= error.text %></p>
        </div>
      <% }%>
    <section class="card-form">
        <img src="<%= card.image %>" class="card-form-img">
        <form method="POST" id="edit-form" >
            <input type="hidden" name="image" id="image" value="<%= card.image %>" />
            <input type="hidden" name="cardId" id="cardId" value="<%= card.id %>" />

            <div class="input-group" id="name-input">
                <label for="card-name">Name <img src="/images/non-edit.svg" class="helper-img"></label>
                <input
                name="cardName"
                id="name"
                value="<%= card.name %>"
                readonly
                required
                />
            </div>

            <div class="input-group">
                <label for="card-game">Game <img src="/images/non-edit.svg" class="helper-img"></label>
                <input
                name="cardGame"
                id="game"
                value="<%= card.game %>"
                readonly
                required
                />
            </div>
 
            <div class="input-group" id="condition-input">
                <label for="condition">Condition</label>
                <select name="condition" id="condition" required>
                    <option value="" disabled selected hidden></option>
                    <option value="Mint" <%= card.condition === "Mint" ? "selected": "" %>>Mint</option>
                    <option value="Near Mint" <%= card.condition === "Near Mint" ? "selected" : "" %>>Near Mint</option>
                    <option value="Lightly Played" <%= card.condition === "Lightly Played" ? "selected" : "" %>>Lightly Played</option>
                    <option value="Moderately Played" <%= card.condition === "Moderately Played" ? "selected" : "" %>>Moderately Played</option>
                    <option value="Heavily Played" <%= card.condition === "Heavily Played" ? "selected" : "" %>>Heavily Played</option>
                    <option value="Damaged" <%= card.condition === "Damaged" ? "selected" : "" %>>Damaged</option>
                </select>
            </div>

            <div class="input-group" id="foil-input">
                <label for="is-foil">Foil</label>
                <input type="checkbox" name="isFoil" id="is-foil" <%= card.is_foil ? "checked" : "" %>>
            </div>

            <div class="input-group" id="quantity-input">
                <label for="quantity">Quantity</label>
                <div class="increment">
                    <button type="button" class="increment-btn" id="decrease">-</button>
                    <input type="number" name="quantity" id="quantity" value="<%= card.quantity %>" required/>
                    <button type="button" class="increment-btn" id="increase">+</button>
                </div>
            </div>

            <button onclick="openPasswordCheck(`Edit`, `<%= card.id %>`, `<%= card.name %>`)" type="button">Save changes</button>
        </form>
    </section>
</body> 
</html>
<%-include("./partials/passwordCheck.ejs") %>
<script src="/utils/quantity.js"></script>  